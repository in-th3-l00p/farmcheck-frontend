import style from "./cart.module.scss";

const Cart = () => {
    return (
        <div className={style.cart}>
            <h2 className={style.title}>Your cart</h2>
        </div>
    );
};

export default Cart;
