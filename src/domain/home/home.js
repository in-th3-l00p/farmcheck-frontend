/**
 * Page at path "/"
 * @returns page's component
 */

import ImageCarousel from "../../components/carousel/carousel"

export const Home = () => {
    return (
        <ImageCarousel/>
    );
}

export default Home;